# ── 私有内部文档（框架公开·交付私有：绝不进公开仓库） ──────────────
# 这三份是内部开发/商业规划文档，含变现策略、alpha「留手」打法、私有母体路径。
# 只有 README.md 是对外说明文档。发布前务必 `git status` 确认看不到它们。
VibeResearch-开发日志.md
VibeResearch-方案定稿.md
VibeResearch-专业化建议.md

# ── 私有数据（持仓 / 关注股 / 缓存：只存本地、不上传） ──────────────
# E2E 沙箱数据目录。验收脚本会往里写假持仓，绝不能入库；
# 它的存在正是为了让 E2E 碰不到 ~/.vibe-research/ 里的真实数据。
.sandbox-data/
**/.cache/
my-watchlist.json
portfolio.json
public/user-data/

# ── 密钥 / 环境（绝不进仓库） ──────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.secret

# ── 依赖 / 构建产物 ───────────────────────────────────────────────
**/.venv/
**/node_modules/
**/__pycache__/
**/.pytest_cache/
*.pyc
dist/
build/
frontend/tsconfig.tsbuildinfo
# Playwright 的临时诊断产物（trace / 失败快照）。
# 注意：Harness 验收截图不在这里——它们由 e2e/_helpers.ts 的 shot() 显式写到
# docs/screenshots/VR-GOAL-XXX_<slug>/，那是长期归档、必须入库。
frontend/test-results/
frontend/playwright-report/

# ── 编辑器 / 系统 / 工具 ──────────────────────────────────────────
# .claude/ 里 settings.json + commands/ + hooks/ 是项目 harness，**入库**跟着仓库走；
# 只有本机个性化的 settings.local.json 不进仓库。
**/.claude/settings.local.json
.DS_Store
*.log
.vscode/
.idea/
