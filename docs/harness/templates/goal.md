# VR-GOAL-XXX ｜ <一句话说清做什么>

- **状态**：草稿（等第一道闸）/ 验收项已确认 → 待 Plan 确认 / 实现中 / 待签字 / 已验收
- **档位**：完整闭环 / 轻量
- **创建日期**：YYYY-MM-DD
- **slug**：`<lower-kebab-case>`
- **分支**：`goal/VR-GOAL-XXX_<slug>`（完整档）/ 直接在 dev（轻量档）

## 背景

为什么要做这件事。当前是什么状况、哪里不够用、不做会怎样。
用业务语言写，不写实现方案（那是 Plan 的事）。

## 垂直切片

必须能用一句话说完，超出一句话说明切太大了，拆成多个 Goal。

- **角色**：谁（使用看板的投研用户 / 接入的 AI agent / 部署者）
- **动作**：做什么
- **可见结果**：他能看到什么变化

## 验收项

每条必须**可判真假**，且**写明证据形态**。写不出判据或证据形态的就不是验收项。

| # | 验收项 | 判据 | 证据形态 |
|---|---|---|---|
| 1 | | 具体到「点哪里 → 出现什么 → 多久内」 | 截图 `01_xxx.jpg` |
| 2 | | | 命令输出 |
| 3 | | 无 console error / 无 500 | CI run URL |

> 反例：「体验更流畅」「代码更清晰」——无法判真假。
> 正例：「个股页输入 `AAPL` 回车后 2 秒内出现总市值与 ROE，且 console 无 error」。

证据形态按改动性质选：有界面变化用截图；纯后端 / 基建 / 流程改动用命令输出、
`git log --graph`、文件片段、GitHub Actions run URL。**不要为了凑格式截一张无关的图。**

> **写到这里就停。** 请负责人确认验收项（第一道闸）后再写 Plan。
> 定标准的人不能同时是判分的人——这道闸是整套闭环不退化成自证的前提。

## 不在范围内

明确列出这次**不做**什么，防止范围蔓延。

## 合规检查

- [ ] 不涉及荐股 / 预测涨跌 / 买卖时机 / 主观评分
- [ ] 不把打板原始池（含个股名单）直接暴露到 API 或 UI
- [ ] 新增的用户数据只落本地，不上传、不进仓库
