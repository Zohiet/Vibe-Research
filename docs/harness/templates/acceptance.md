# VR-GOAL-XXX 验收报告 ｜ <标题>

- **Goal Spec**：[`../goals/VR-GOAL-XXX_<slug>.md`](../goals/VR-GOAL-XXX_<slug>.md)
- **实现 Plan**：[`../plans/VR-GOAL-XXX_<slug>.md`](../plans/VR-GOAL-XXX_<slug>.md)
- **送审日期**：YYYY-MM-DD
- **状态**：待签字 → （由负责人填写签字栏）

> **本报告不设「结论：✅ 通过」栏。** 撰写者的职责是把证据摆齐、把没达成的如实标出来；
> **判定权在负责人**，见文末签字栏。验收项是 AI 写的、实现是 AI 做的、验证也是 AI 跑的——
> 再由 AI 下结论，这套闭环就成了自己出卷自己判分。

---

# 一、业务验收（正文）

用业务语言写，对着 Goal Spec 的验收项逐条**列出证据**。**不要在这一节堆命令行输出。**

## 做成了什么

一段话说清：现在能做到什么以前做不到的事。

## 逐条证据

| # | 验收项 | 达成情况 | 证据 |
|---|---|---|---|
| 1 | | 已达成 | [`01_xxx.jpg`](../screenshots/VR-GOAL-XXX_<slug>/01_xxx.jpg) |
| 2 | | 已达成 | 见附录「命令输出」第 N 段 |
| 3 | | **未达成 / 部分达成** | 说明差在哪、原因、是否可接受 |

> 「达成情况」只陈述事实（已达成 / 未达成 / 部分达成 + 差距），不写「通过」——
> 那是签字栏的事。**不要为了好看把未达成的写成部分达成。**

## 与 Plan 的偏差

实现过程中偏离 Plan 的地方 + 原因。没有就写「无」。
**发现了 Plan 没预料到的问题，写在这里**——这是下次写 Plan 时最值钱的输入。

## 遗留与后续

已知没做完 / 故意留到下个 Goal 的事。

---

# 二、工程追溯证据（附录）

## CI（独立证据）

**GitHub Actions run**：<粘贴 run URL —— 这是独立于本机的证据，优先引用它>

本机 `./ci.ps1`（辅助，自述性质）：

```
$ ./ci.ps1
<粘贴关键输出>
```

- 前端 `tsc -b`：通过 / 失败
- 后端 `pytest -m "not live"`：`N passed`（**应为零 failed，本项目无豁免白名单**）
- 后端 import 自检：`N` 条路由

## 验收证据

有界面变化的：

```
$ npx playwright test e2e/VR-GOAL-XXX_<slug>.spec.ts
<粘贴输出>
```

截图归档目录：`docs/screenshots/VR-GOAL-XXX_<slug>/`

纯后端 / 基建改动的：直接粘贴对应的命令输出、`git log --graph`、文件片段。

## 改动文件

```
$ git diff --stat <base>..<head>
```

## 关键提交

| sha | 说明 |
|---|---|
| | |

## diff 复查

- [ ] 改过的 API 的所有调用方都已跟进（grep 过）
- [ ] 没有误入库的临时文件 / 密钥 / 用户数据
- [ ] 合规红线未被触碰

---

# 三、验收签字

> **以下由负责人填写。** 撰写者不得代填。

- **结论**：⬜ 通过　⬜ 不通过　⬜ 有条件通过（条件：________）
- **签字**：
- **日期**：
- **备注 / 要求的后续动作**：

签字通过后才可并回 `dev`（`--no-ff`）并走 `/vr-release` 发布到 `main`。
