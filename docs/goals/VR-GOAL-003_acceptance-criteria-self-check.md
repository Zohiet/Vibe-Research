# VR-GOAL-003 ｜ 给验收项加一条自检：判据不能依赖会被本 Goal 搅浑的量

- **状态**：已实现，待签字（验收项已于 2026-07-30 过第一道闸）
- **档位**：轻量（给流程加一条规则，有行为变化——不属于「无行为变化整理」的豁免范畴）
- **创建日期**：2026-07-30
- **slug**：`acceptance-criteria-self-check`
- **分支**：直接在 `dev` 上（轻量档不开分支）

## 背景

VR-GOAL-001 和 002 各有一条验收项没能达成，而且**是同一类错误**——判据依赖了会被
本 Goal 自身改动影响的量：

| Goal | 验收项 | 判据 | 为什么栽 |
|---|---|---|---|
| 001 | 3 | 「`goal/*` + `--no-ff`」的证据是合并后的历史形状 | **合并发生在验收之后**，验收当时那个证据还不存在 |
| 002 | 3 | 「CLAUDE.md 净减行」 | **同一个 Goal 也在往这个文件加必需的新内容**，行数把「删重复」和「加新内容」混成了一个数 |

两次都不是实现没做到，是**判据本身写坏了**。两次都只能在验收报告里长篇解释、交负责人
特批通过——这种"解释型通过"多来几次，验收就失去约束力了。

连续两个 Goal 栽在同一处，说明这不是偶然，是写 Goal Spec 时缺一道自检。

## 垂直切片

- **角色**：写 Goal Spec 的人（通常是 AI）
- **动作**：在定验收项时多问一句「这个判据会不会被我自己的改动搅浑」
- **可见结果**：不再出现「实现做到了但判据没法判」的验收项

## 验收项

| # | 验收项 | 判据 | 证据形态 |
|---|---|---|---|
| 1 | `goal_workflow.md` 的 Goal Spec 一节加入判据自检 | 该节明确要求：写完每条验收项要检查「判据依赖的量，是否会被本 Goal 自身的改动影响」；并给出**时序污染**与**度量污染**两类反模式各自的识别方式与改写方向 | 文档片段 |
| 2 | 用真实案例说明，不写抽象规则 | 两类反模式各附 001 / 002 的真实教训（含 Goal 编号），可直接对照 | 文档片段 |
| 3 | `templates/goal.md` 落到动作上 | 验收项表格下方加一行自检提示，让写模板的人当场就能想起来 | 文档片段 |
| 4 | 不引入新的重复 | 该规则只在 `goal_workflow.md` 定义一次，`templates/goal.md` 只放一句提示 + 指针，不复述 | `grep` 输出 |
| 5 | 验证仍全绿 | `./ci.ps1` 退出码 0 | 命令输出 |

## 不在范围内

- 不回头改 001 / 002 已签收的验收报告——那两条已经过负责人判定通过，改了反而破坏记录
- 不改任何代码、配置、CI

## 合规检查

- [x] 不涉及荐股 / 预测 / 评分——纯流程规则
- [x] 不涉及用户数据

---

## 验收判定

> 轻量档的验收判定追写在本文档末尾（不单独出验收报告）。撰写者填证据，负责人签字。
> **撰写者不下结论**——理由同完整档：定标准、做实现、跑验证都是同一方。

**送审日期**：2026-07-30

| # | 验收项 | 达成情况 | 证据 |
|---|---|---|---|
| 1 | `goal_workflow.md` 加入判据自检 | 已达成 | §1 Goal Spec 下新增「#### 判据自检：这个量会不会被我自己的改动搅浑？」，含两类反模式各自的识别方式与改写方向，另附一句「聚合量只有在本 Goal 对它影响单向时才安全」 |
| 2 | 用真实案例说明 | 已达成 | 时序污染引 VR-GOAL-001 验收项 3（证据是合并后的历史，而合并在签字之后）；度量污染引 VR-GOAL-002 验收项 3（净减行被新内容污染，-33/+40） |
| 3 | `templates/goal.md` 落到动作上 | 已达成 | 验收项表格下方加了一段 `> ⚠️ 逐条自检…` 提示，写模板时当场可见 |
| 4 | 不引入新重复 | 已达成 | 见下方 grep：完整定义（「改写方向」2 处 + 「真实案例」2 处）只在 `goal_workflow.md`；模板里只有 1 处「改写方向」且是指针句，0 处真实案例 |
| 5 | 验证全绿 | 已达成 | `./ci.ps1` exit=0：tsc 通过 / 86 passed / 53 条路由 |

### 证据

**验收项 4（不重复）**

```
$ grep -rn "时序污染\|度量污染" docs/ CLAUDE.md | grep -v "^docs/goals/"
docs/harness/goal_workflow.md:123:**① 时序污染——判据依赖验收之后才发生的事。**
docs/harness/goal_workflow.md:133:**② 度量污染——用一个会被本 Goal 其他改动一起影响的聚合量。**
docs/harness/templates/goal.md:39:> 两类要防：判据依赖「合并后 / 发布后」才存在的东西…
docs/harness/templates/goal.md:40:> 覆盖率这类聚合数字而本 Goal 还会从别的方向影响它…

文件                                    改写方向  真实案例
docs/harness/goal_workflow.md              2        2      ← 完整定义在这里
docs/harness/templates/goal.md             1        0      ← 只是提示 + 指针
```

**验收项 5（CI）**

```
=== 前端类型检查 (tsc -b) ===   ✓ 通过
=== 后端离线测试 ===            86 passed, 11 deselected   ✓ 全部通过
=== 后端 import 自检 ===        ✓ 通过，53 条路由
=== 汇总 ===  CI 全绿 ✓   exit=0
```

### 与 Goal Spec 的偏差

无。五条验收项按原样达成，未出现需要解释的情况。

> 顺带一提：本 Goal 的验收项是**在新规则写下来之前**定的，但已经下意识按它写了——
> 五条判据全部是「文档里有没有这段」「grep 结果如何」「CI 退出码」这类当场可查、
> 且不受本 Goal 其他改动干扰的量。这算是这条规则的第一次实地检验。

### 验收签字

> **以下由负责人填写。** 撰写者不得代填。

- **结论**：⬜ 通过　⬜ 不通过　⬜ 有条件通过（条件：________）
- **签字**：
- **日期**：
- **备注 / 要求的后续动作**：
