# VR-GOAL-006 ｜ 持仓明细行内加仓/减仓，买卖统一为可撤销的交易流水

- **状态**：验收项已确认（2026-07-30，第一道闸通过）→ 待 Plan 确认
- **档位**：完整闭环
- **创建日期**：2026-07-30
- **slug**：`holdings-inline-operations`
- **分支**：`goal/VR-GOAL-006_holdings-inline-operations`

## 背景

持仓页现在有三个问题，都源于**「持仓」和「已清仓」是两套互不联动的操作**：

1. **减仓功能根本不存在**。没有任何 API 能减少股数，只能 `remove_holding` 整笔删掉、再 `add_holding` 添回剩余股数——而且要原样重填加权成本，填错一点后面所有盈亏就不准了。
2. **`close_position` 只往 `closed` 追加记录，从不碰 holdings**。卖出后必须手工维护两边。
3. **加仓不留任何痕迹**，与卖出记录不对称。

真实数据印证了这个痛点：`688253`（英诺特）同时出现在持仓（1795 股）和已清仓（2516 股）里，`588170` 也是——都是部分卖出后手工维护两边的产物。

第 3 条还藏着一个洞：**加仓填错了无法干净撤销**。唯一办法是减仓，但减仓会记一笔卖出并计入已实现盈亏——一笔根本没发生的交易污染了盈亏历史。

## 垂直切片

- **角色**：记账的投研用户
- **动作**：在持仓明细的行内直接加仓 / 减仓
- **可见结果**：股数与加权成本自动更新，同时生成一条可撤销的交易流水；不再需要手工对账

## 已收敛决策

> 本 Goal 的第一道闸以 `/superpowers:brainstorming` 形式进行（与 `grilling` 同等作用：
> 把开放决策逐条问到收敛）。以下是收敛结果。

| # | 议题 | 裁决 | 理由 / 取舍 |
|---|---|---|---|
| 1 | 减仓的已实现盈亏按什么成本算 | **当前持仓的加权平均成本** | 与 `add_holding` 现有口径同源，不引入新数据结构；FIFO 要记录每笔买入历史，A 股个人投资者用不上 |
| 2 | 减到 0 股怎么办 | **自动移除持仓，并删掉「添加清仓记录」表单** | 清仓是减仓的特例。用户的 7 条历史记录都是"卖出时顺手记"，不是"事后补记陈年旧账"，为不做的场景留表单不划算 |
| 3 | 交互形态 | **行内展开**（点按钮，行下方展开表单） | 操作时能同时看到该行的现价/数量/成本，弹窗会盖住；`Notes.tsx` 已有同款展开模式可照抄；省掉弹窗的焦点管理与遮罩处理 |
| 4 | 买卖记录是否合成一张表 | **合成 `transactions` 流水表**，持仓仍单独存 | 用户提出。修掉了「加仓无法干净撤销」的洞；买卖对称。**不做彻底事件溯源**——已有持仓的买入历史在数据里根本不存在，无法迁移，只能伪造 |
| 5 | 撤销机制 | **每笔交易存操作前的持仓快照，撤销 = 写回快照**；限制为「该代码的最新一笔」 | 快照还原精确，避免反推加权平均的浮点漂移。副产品：历史 7 条无快照，天然不可撤销，**撤销条件统一成一条规则**（有快照 && 是最新一笔），不需要 `from_reduce` 之类的来源标记，前端也不用加「来源」列 |

补充口径（收敛过程中一并定下，无争议）：

- 卖出价默认填当前现价、可改；日期默认今天
- 加仓表单只有股数 + 买入价，无日期字段（不产生已实现盈亏）
- 「已清仓」列表改名 **「交易记录」**；撤销按钮文案用「撤销」不用「删除」
- 已有 4 条持仓在流水里没有对应 buy 记录，**不伪造**，列表顶部注明「流水自 2026-07-30 起记录」
- 行内 🗑 删除按钮保留——语义是「这条记录本身是错的」，与减仓（真的卖了）不同

## 设计

### 数据模型

```jsonc
{
  "holdings": [{ "code", "shares", "cost" }],   // 结构完全不变
  "transactions": [{
    "id",                                        // 删除用 id，不用 index
    "code", "name", "date",
    "type": "buy" | "sell",
    "shares", "price",
    "prev_shares", "prev_cost",                  // 操作前的持仓快照；迁移来的历史记录无此字段
    "pnl", "pnl_pct"                             // 仅 sell 有
  }]
}
```

### 后端

| 端点 | 变化 |
|---|---|
| `POST /api/portfolio/holding` | 加仓。加权平均合并逻辑不变，**额外写一条 `type:buy` 流水 + 快照** |
| `POST /api/portfolio/reduce` | **新增**。入参 `{code, shares, price, date}`；后端自取加权成本；减到 0 移除持仓；写 `type:sell` 流水。全程在既有 `_LOCK` 内一次 `_save` |
| `DELETE /api/portfolio/transaction/{id}` | **新增**，撤销 = 把 `prev_shares/prev_cost` 写回持仓 |
| `GET /api/portfolio` | 返回字段 `closed` → `transactions`；`realized_pnl` 只从 `type:sell` 累加 |
| `POST /api/portfolio/close` | **删除**。「添加清仓记录」表单已去掉（决策 #2），此端点无调用方 |
| `DELETE /api/portfolio/close` | **删除**。由 `/transaction/{id}` 取代 |

**「最新一笔」的判定**：按 `transactions` 数组中该 `code` **最后出现的那条**（追加顺序），
不按 `date` 字段——同一天可能有多笔，日期分不出先后。

**交易记录列表排序**：按数组顺序**倒序**展示（最新在上）。

**为什么减仓必须是一个端点**：它是双写（改 holdings + 加流水）。拆成两次调用，中间失败就是数据不一致——而「不用手工对账」正是本 Goal 的立意。

**迁移**：`_load()` 发现旧 `closed` 字段就地转成 `transactions`（补 id、`type:"sell"`、不补快照）。照 `myreports._migrate_legacy()` 的路子——**先备份再转换，转换失败保持原样**。备份写到同目录 `portfolio.json.bak-<YYYYMMDD-HHMMSS>`，不删除、留给用户自行处置。

### 前端

行内 `[加仓] [减仓]` 按钮 → 行下方展开表单，边填边算「本次已实现盈亏」。股数超过持仓时确认按钮禁用；减到 0 时提示「将清空该持仓」。

「交易记录」列表加类型列，显示买/卖两种；不可撤销的记录不显示撤销按钮；确认框写明「将还原 100 股持仓（成本 1,500）」。

## 验收项

| # | 验收项 | 判据 | 证据形态 |
|---|---|---|---|
| 1 | 减仓正确 | 100 股@成本 1500 减 40 股@价 1600：持仓剩 60 股、**成本仍为 1500**、流水多一条 `type:sell`、`pnl == (1600-1500)*40` | pytest |
| 2 | 减到 0 移除持仓 | 减完全部股数后 `holdings` 中无该代码 | pytest |
| 3 | 入参校验 | 减超量 / 减 0 / 减负数 / 代码不存在 → HTTP 400 | pytest |
| 4 | 加仓写流水 | 加仓后流水多一条 `type:buy`，且带 `prev_shares`/`prev_cost` 快照 | pytest |
| 5 | 撤销 sell 精确还原 | 撤销后 `shares` 与 `cost` 与减仓前**数值完全相等**（`==`，不是近似比较） | pytest |
| 6 | 撤销 buy 精确还原 | 撤销后 `cost` 等于加仓前的值（走快照，非反推） | pytest |
| 7 | 撤销限制生效 | 撤销非最新一笔 → 拒绝；撤销无快照的历史记录 → 拒绝 | pytest |
| 8 | 迁移不改账 | 构造含 7 条旧 `closed` 的数据 → 迁移后 `transactions` 有 7 条 `type:sell`，且 `realized_pnl` 与迁移前**数值相等** | pytest |
| 9 | 行内操作可用 | 沙箱内：建仓 → 加仓 → 减仓 → 撤销四步，界面数字随之变化且无 console error | E2E 截图 4 张 |
| 10 | 旧表单与旧端点已移除 | 持仓页不再有「添加清仓记录」表单；`app.py` 中不再有 `/api/portfolio/close` 的两个端点 | E2E 截图 + `grep` |
| 11 | CI 全绿 | `./ci.ps1` 与 `./ci.ps1 -E2E` 退出码 0；GitHub Actions run 为绿 | 命令输出 + run URL |

> **判据自检**（VR-GOAL-003 规则）：11 条全部是当场可查的具体状态或数值比较，
> 无「合并后 / 发布后」才存在的证据（时序污染），无会被本 Goal 其他改动一起影响的
> 聚合指标（度量污染）。验收项 8 用的是「迁移前后数值相等」这种成对比较，
> 不受本 Goal 新增交易的干扰。

## 不在范围内

- 不做彻底事件溯源（持仓由流水推导）——见已收敛决策 #4
- 不做编辑已有交易记录，只有撤销
- 不做撤销栈 / 操作日志
- 不改 AI 上下文（`aiContext` 只用 holdings，本 Goal 不碰）
- 不给已有 4 条持仓伪造 buy 记录

## 合规检查

- [x] 不涉及荐股 / 预测涨跌 / 买卖时机 / 主观评分——只是记账工具
- [x] 不把打板原始池暴露到 API 或 UI——不涉及
- [x] 新增数据只落本地 `~/.vibe-research/portfolio.json`，不上传、不进仓库

## ⚠️ 实施期间的安全要求

本 Goal 直接改动持仓数据结构，且 E2E 会写数据。

1. 实施前备份 `~/.vibe-research/portfolio.json`，记录 `holdings`/`closed` 语义指纹
2. E2E **必须**跑在沙箱（`./dev.ps1 -Sandbox`），脚本第一行 `assertSandbox(page)`
3. 迁移逻辑先在沙箱与临时目录验证，确认无误后才让真实数据经过它
4. 收工前比对真实数据指纹；不一致立即从备份恢复并停下报告
