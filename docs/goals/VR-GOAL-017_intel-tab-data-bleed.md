# VR-GOAL-017 ｜ 资讯页切 tab 不再显示上一个 tab 的数据

- **状态**：已实现待复核（2026-08-03）
- **档位**：轻量（既有行为的修正，不新增端点 / 页面 / 数据格式）
- **创建日期**：2026-08-03
- **slug**：`intel-tab-data-bleed`
- **分支**：直接在 dev（轻量档）

## 背景

用户报告资讯页「A股公告」与「公开新闻」两个 tab **内容完全一致**，而且列表
**每隔几秒自动刷新一次、然后又恢复**——他以为是哪里混进了自动刷新（那个已剔出范围的功能）。

**没有任何定时器。** 全仓库的定时器只有两处：`useLiveQuotes`（10s，只用于关注股页）
与 `Portfolio.tsx`（30min）。资讯页一个都不沾。两个症状是**同一个 bug 的两面**。

### 根因（已确定性复现）

先排掉数据层：直接打两个端点，返回内容毫无重合——公告是 `date/title/type/url`，
新闻是六键中文契约，标题一条都不一样。**后端没问题。**

`Intel.tsx:353-358` 的渲染分支：

```tsx
{cur.key === "investment-news" ? <InvestmentNewsPanel />
 : cur.key === "filings"       ? <WatchlistFeed kind="filings" />
 : cur.key === "news"          ? <WatchlistFeed kind="news" />
 : <>…</>}
```

后两个分支是**同一个组件类型、占同一个子位置、没有 `key`**。React 的 reconciliation
判定「同一个实例、只是 props 变了」→ **不重挂载**，`rows` state 原样留着。

叠加第二个缺陷：`load()`（`Intel.tsx:223-271`）**没有任何取消或时序守卫**，
promise 一落地就无条件 `setRows`——上一个 tab 遗留的请求照样能盖掉当前 tab。

### 实测复现（page.route 打桩，两边给不同标题 + 2.5s 延迟，不碰网络）

```
① 公告 tab 已出数据
② 刚切到新闻 tab，公告内容还在？ true
   顶部计数： 关注 1 只 · 共 1 条新闻（近期）     ← 标签写着"新闻"，列表里是公告
③ 几秒后新闻数据到达，列表内容被替换              ← 用户说的"自动刷新"

④ 停在「A股公告」tab，此时新闻请求仍在飞
   t≈1.0s  公告=false 新闻=true                  ← 迟到的新闻响应盖进了公告 tab
   t≈2.5s  公告=true  新闻=false                 ← 用户说的"又恢复"
   t≈4.0s  公告=true  新闻=false
```

②那行还暴露一个加重因素：**连转圈都不给**。加载态条件是 `loading && rows.length === 0`，
而 rows 非空（上一个 tab 的），所以界面上没有任何"正在加载"的信号——
上一个 tab 的数据看起来就是这个 tab 的真数据。

延迟实测用的 2.5s；**真实使用下更久**：`em_get` ≥1s 串行限流，关注 N 只股票
一次 load 要 N 秒以上，两个 tab 的请求在同一条队列里排，重叠窗口很长。

### 这不是 VR-GOAL-016 引入的

`git log -L 216,275 / 353,359` 显示这段代码**自 2026-07-05 首次发布起未被改动过**。
但 016 是它变明显的原因：016 之前 `/api/news` 对每个代码都 502，新闻 tab 必然落到空列表，
串味只闪一下就变成「暂无新闻」；016 修好之后两边都有数据，串味才呈现成「两处一模一样」。

### 仓库里已有的正确做法

`StockData.tsx:106` 有现成的竞态守卫 `runIdRef`（注释：「快速换代码时只让最新一次回填」），
每次 `run()` 自增，回填前比对 `rid === runIdRef.current`。**Intel 没用这套。**

## 垂直切片

- **角色**：使用看板的投研用户
- **动作**：在资讯页的「A股公告」与「公开新闻」之间来回切
- **可见结果**：每个 tab 只显示自己的数据；正在拉取时看到加载态而不是别人的数据

## 验收项（已确认 2026-08-03）

| # | 验收项 | 判据 | 证据形态 |
|---|---|---|---|
| 1 | 切 tab 后不显示上一个 tab 的数据 | 公告 tab 出数据后点「公开新闻」，300ms 内公告条目**不可见** | E2E 断言 + 截图 `01_切tab后不串味.jpg` |
| 2 | 拉取期间有加载态 | 同一时刻「正在汇总关注股的新闻…」**可见** | 同上截图 |
| 3 | 迟到的响应盖不进来 | 让新闻请求在切回公告 tab 之后才落地，全程公告 tab **从不出现新闻条目** | E2E 断言（三个时间点采样） |
| 4 | 各 tab 自己的数据仍能正常出现 | 切到新闻 tab 后 15s 内新闻条目可见 | E2E 断言 + 截图 `02_新闻tab出自己的数据.jpg` |
| 5 | `/api/news` 有进程内缓存 | 同一 code 连打两次，上游 `astock.stock_news` **只被调用 1 次** | pytest |
| 6 | 「刷新」能穿透缓存 | 带 `force=1` 再打一次，上游**再被调用 1 次**且拿到新内容 | pytest |
| 7 | 失败不进缓存 | 上游抛异常 → 502，且下一次请求**仍会重试上游**（不是把错误缓存住） | pytest |
| 8 | 前端「刷新」按钮确实带上了穿透 | 点刷新时发出的请求 URL 含 `force=1`；页面自动加载的那次**不含** | E2E 断言（拦截请求记 URL） |
| 9 | 变红实验：撤掉修复，对应回归测试**变红** | 去掉 `key` → 验收项 1/3 变红；去掉 force 透传 → 验收项 8 变红 | 命令输出（撤前撤后各一次） |
| 10 | 没弄坏别的 | `./ci.ps1 -E2E` 全绿，无 console error | CI 输出 + GitHub Actions run URL |

> 判据自检：十条都不依赖「合并后 / 发布后」才存在的东西（无时序污染），
> 也不用行数 / 覆盖率这类会被本 Goal 从别处影响的聚合量（无度量污染）。
> 第 1/3/4 条用 `page.route` 打桩固定了延迟，第 5~7 条打桩上游函数，
> **都不受实时行情快慢与东财风控干扰**。

## 已收敛决策（拷打于 2026-08-03）

| # | 议题 | 裁决 | 理由 / 取舍 |
|---|---|---|---|
| 1 | 修到多深 | **只加 `key={kind}`，不加竞态守卫** | 实测 `key` 单独一条即可消掉两个症状。同 tab 内并发 load **发生不了**（`Intel.tsx:291` 刷新按钮 `disabled={loading}`），加守卫就是加一段永不进入的分支——`CLAUDE.md`「从不执行的代码就是 bug 藏身处」。守卫防的那个假想改动真发生时，验收项 1/3 的回归测试本来就会变红 |
| 2 | 「切 tab 要重拉」治不治 | **治，但治在后端** | ⚠️ 起草时我量化成「每切一次等 13 秒」，**是错的**：`app.py:640` 的 `_ANN_CACHE` 早有 15 分钟缓存，公告 tab 秒回。这批端点里**只有 `/api/news` 漏了缓存**（VR-GOAL-016 重写它时只管修 502，没对齐邻居）。用户要的「后端不关就还在」= 进程内存缓存，仓库到处都是这个模式，补漏即可 |
| 3 | 前端要不要也缓存一份 | **不要** | 系统已有缓存层，再叠一层就是两处缓存、两套失效规则，以后对不上要查两遍。前端保持无状态，切页面回来照常请求、后端秒回。也不走 `aisession`——`CLAUDE.md:172` 写死「只存 AI 产出，UI 状态不要放进来，端点特意叫 `aisession` 而不是 `uistate`」 |
| 4 | 刷新按钮怎么办 | **让它穿透缓存**，照 `/api/radar/refresh` 的形状 | 拷打里挖出来的既有毛病：公告 tab 的「刷新」**15 分钟内点了纹丝不动**，今天就是这样，只是被串味掩盖了。一个点了不干活的按钮比没有按钮更糟。代价诚实记：穿透后每只股票重排 ≥1s 队列，点一次真的要等 N 秒——但这正是「人手点才重拉」要的语义 |
| 5 | `/api/news` 的 TTL | **15 分钟**，与 `/api/announcements` 对齐 | 同一页面上的两个 tab 若用两个 TTL，「为什么这个变了那个没变」就解释不了 |
| 6 | 个股页的「查询」要不要也穿透 | **不要** | 它的语义是"查这只股票"不是"重抓"；一次要拉 15+ 个端点，全穿透会把限流队列彻底堵死（本来就 12~16 秒）。且个股页对公告**今天就是这个行为**，新闻跟上只是对齐 |
| 7 | 三件事要不要拆成多个 Goal | **不拆** | 照 VR-GOAL-015 先例：轻量档不开分支、没有合并点，三件事本来就是三个独立提交，撤哪个都是一条 `git revert <sha>` |

## 不在范围内

- **`em_get` 的 ≥1s 串行限流本身**——那才是「个股页 12~16 秒」的真瓶颈，要治得改并发
  （分域名令牌桶之类），风险等级高。已是 backlog 第一条，单开 Goal。
- **个股页「查询」的缓存穿透**（决策 6）。
- **前端任何形式的缓存**（决策 3）。
- **竞态守卫 `runIdRef`**（决策 1）。

## 合规检查

- [x] 不涉及荐股 / 预测涨跌 / 买卖时机 / 主观评分
- [x] 不把打板原始池直接暴露到 API 或 UI
- [x] 不新增任何落盘数据

---

# 验收判定（2026-08-03）

**完成日期**：2026-08-03　**提交**：`7cf49f2`

> 位置说明：本 Goal 是**轻量档**，按 `goal_workflow.md` 完成定义
> 「轻量档只需：Goal Spec（含验收判定）+ `./ci.ps1` 全绿 + 证据」，
> 验收判定追写在本文件末尾，**不产生 `docs/acceptance/` 下的文件**。

`./ci.ps1 -E2E` 全绿：tsc 通过 / **186 passed**（新增 9 条）/ **18 E2E passed**。
GitHub Actions：<https://github.com/Zohiet/Vibe-Research/actions/runs/30780641020>（success）。

## 逐条

| # | 验收项 | 达成情况 | 证据 |
|---|---|---|---|
| 1 | 切 tab 后不显示上一个 tab 的数据 | 已达成 | E2E「切 tab 不串味」：点「公开新闻」后 `expect(ANN_TITLE).toBeHidden()`。截图 `01_切tab后不串味.jpg`——卡片标题是「公开新闻」、计数「共 0 条新闻」、列表区为加载态，**没有任何公告条目** |
| 2 | 拉取期间有加载态 | 已达成 | 同一断言块里「正在汇总关注股的新闻…」可见；同上截图肉眼确认 |
| 3 | 迟到的响应盖不进来 | 已达成 | E2E「迟到的响应盖不进来」：切回公告 tab 后装 MutationObserver 连续观察，`__seen === false`。⚠️ 第一版用 `toBeHidden()` 采样，**撤掉 key 后依然是绿的**（见下） |
| 4 | 各 tab 自己的数据仍能正常出现 | 已达成 | 截图 `02_新闻tab出自己的数据.jpg`：「共 1 条新闻」+ 条目「新闻条目BBB」 |
| 5 | `/api/news` 有进程内缓存 | 已达成 | `test_news_second_call_hits_cache`：连打两次，上游 `stock_news` 只被调 1 次 |
| 6 | 「刷新」能穿透缓存 | 已达成 | `test_news_force_bypasses_cache`（调 2 次且拿到新那份）+ `test_announcements_force_bypasses_cache`；另有 `test_news_force_refreshes_the_cache` 验证穿透结果**写回**缓存，否则会出现「刷新完是新的、切个 tab 又变回旧的」 |
| 7 | 失败不进缓存 | 已达成 | `test_news_failure_is_not_cached` / `test_announcements_failure_is_not_cached`：首次 502 后第二次照常重试上游 |
| 8 | 前端「刷新」确实带上穿透 | 已达成 | E2E：自动加载发出的 `/api/announcements` 请求**全部不含** `force=1`；点刷新时 `waitForRequest` 捕到含 `force=1` 的请求 |
| 9 | 变红实验 | 已达成 | 见下表，四组全部如期变红 |
| 10 | 没弄坏别的 | 已达成 | 186 passed + 18 E2E；Actions run 绿；E2E 用 `watchConsole` 断言无 console error |

## 变红实验

| 撤销什么 | 期望变红 | 实际 |
|---|---|---|
| 去掉两个 `key` | E2E 1（串味）、E2E 2（迟到响应） | ✅ 两条都红 |
| `refresh` 不传 `force` | E2E 3（force=1） | ✅ 红 |
| 后端 `_NEWS_CACHE` 永不命中 | `test_news_second_call_hits_cache` 等 | ✅ 2 条红 |
| 后端忽略 `force` 参数 | 三条 force 用例 | ✅ 3 条红 |

## 实现中发现的问题（Spec 没预料到的）

### ① 两条测试是绿的，但什么都没验

**都是变红实验抓到的，不是跑绿的时候。**

- **「刷新带 force」那条**：点完刷新我断言 `ANN_TITLE` 可见——而它**本来就可见**
  （首次加载留下的），这条断言瞬间通过，随后读 `urls` 时请求还没离开浏览器。
  `--repeat-each=4 --retries=0` 下 **4 次挂 3 次**，第一次跑绿纯属撞运气。
  改成 `waitForRequest` 等「请求真的发出去」这个语义事件。
- **「迟到响应盖不进来」那条**：我用 `expect(...).toBeHidden()` 每 1.2s 采样一次。
  但那个断言**会自动重试**，而本 bug 的表现恰好是「闪一下又被盖回去」——
  新闻在 t≈2.5s 出现、t≈3.0s 被公告顶掉，重试窗口里等到「最终隐藏」就判过，
  **把要抓的那一闪当成没发生**。撤掉 `key` 之后这条依然是绿的。
  改成 `MutationObserver` 连续观察 DOM。

这和 VR-GOAL-015 那次日志写两遍是同一类：**判据表述得对，但它能被错误状态满足**。
区别是这次靠变红实验抓到了——15 那次是靠肉眼看输出。**变红实验不是走过场，
它是唯一能证明"绿"有意义的手段。**

### ② 拷打推翻了我起草时的两个判断

- 我在 Q2 里量化成「每切一次 tab 等 13 秒」并推荐"接受现状"。用户一句
  「应该是人手点才会重拉啊」把它推翻，追下去才发现**我的量化本身是错的**：
  `app.py:640` 的 `_ANN_CACHE` 早有 15 分钟缓存，公告 tab 秒回，
  这批端点里**只有 `/api/news` 漏了**。
- 顺着这条又挖出一个既有毛病：**公告 tab 的「刷新」按钮 15 分钟内点了纹丝不动**，
  一直在骗人。它不在原 Spec 里，是拷打的产物。

### ③ 一次操作失误

做变红实验时我用 `git checkout -- frontend/src/pages/Intel.tsx` 还原，
而那些改动**尚未提交**，四处改动被一起冲掉、只能重做。
变红实验要还原的是"我刚手改的那一处"，应当用备份副本（`cp` 一份再改），
**不能用 git 还原未提交的工作**。

## 复核要点（三十秒版）

1. **没有未达成项**，10 条全部有证据。
2. **最值得看的是「两条假绿测试」那节**——两条都写得像模像样、都绿过，
   而且失效方式不同（一条抢时序、一条被自动重试吃掉）。
   如果只跑绿不做变红实验，这次交付的是两条永远不会红的测试。
3. **范围比 Spec 起草时大**：从"前端加个 key"扩到"后端补缓存 + 加穿透"。
   都是拷打里定的（决策 2/4），但 `/api/announcements` 与 `/api/news`
   **对所有调用方生效**——个股页也拉这两个，值得看一眼 diff。
4. **新引入的可见行为**：点「刷新」现在真的会重抓，关注 N 只股票就要等 N 秒以上
   （以前 15 分钟内是秒回但不更新）。这是决策 4 明确要的语义，但体感上会觉得变慢。
5. 未动 `em_get` 的 ≥1s 串行限流——那才是真瓶颈，仍是 backlog 第一条。

**如需撤销**：`git revert 7cf49f2`。轻量档直接提交在 dev 上，无合并点。
