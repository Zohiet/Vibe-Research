# VR-GOAL-018 ｜ 资金流向：主源挂了要有能顶上的备胎，且失败看得见

- **状态**：已实现待复核（2026-08-03）
- **档位**：轻量（既有行为的修正：换/补数据源 + 让失败可见，不新增端点 / 页面 / 数据格式）
- **创建日期**：2026-08-03
- **slug**：`fund-flow-fallback`
- **分支**：直接在 dev（轻量档）

## 背景

用户在多空辩论的事实底稿里看到：

```
未取到：资金流向（双方立论时不得臆测这部分）
```

## 实测到的事实（2026-08-03）

| 源 | 域名 | 结果 |
|---|---|---|
| 主源 `astock.stock_fund_flow_120d` | `push2his.eastmoney.com` | **0 行**。整机 `RemoteDisconnected`——同域名的 K 线端点一样挂，**直连与系统代理都不通**，去掉 Referer/Origin 也不通 |
| 备胎 `tools._fund_flow_today` | `push2delay.eastmoney.com` | 1 行（**只有当天**，无历史） |
| `a-stock-data/SKILL.md:2888` 记录的备胎 | `vip.stock.finance.sina.com.cn` | **60 行完整历史** |
| 对照 | `push2.eastmoney.com` | 正常（茅台 1358.98） |

**坏的是 `push2his` 这一个主机**，不是代码、不是参数、不是代理模式。

⚠️ **当前备胎是通的**，所以用户那次是 `push2his` 与 `push2delay` **两条东财线路同时挂**。
本 Goal 不是修一次抖动，是修「抖动为什么会整块失掉、且失掉了看不出来」。

## 四个结构性问题

| # | 问题 | 位置 | 波及面 |
|---|---|---|---|
| 1 | **异常被吞掉返回 `[]`**，「连不上」与「真的没数据」在上层完全一样 | `astock.py:729` | 同款写法另有 4 处，其中 `eastmoney_datacenter`（:650）背后是 **6 个端点**（两融/大宗/股东户数/分红/龙虎榜/解禁/板块） |
| 2 | **空结果被缓存 15 分钟**，一次抖动锁死、连重试机会都没有 | `app.py:796` `_cached` 无条件写缓存 | **10 个端点**共用它。⚠️ `market.py` 里**同名**的 `_cached` 是**有** `valid` 守卫的——两个同名函数，一个有一个没有 |
| 3 | **备胎只在 tools 层，HTTP 端点没有** | `app.py:852` 只调主源 | 辩论底稿（走 tools）能拿到当日数据，个股页（走 `/api/fund-flow`）是空的——**同一份数据两条路径两种结果** |
| 4 | **两个源同属东财，一个风控面** | `push2his` + `push2delay` | 一起挂就整块没有。仓库自带的新浪备胎（不同域名、不同风控面、给 60 天历史）**躺在 `SKILL.md` 里没接上**，而 `CLAUDE.md` 明确要求「需要的端点先查 `a-stock-data/SKILL.md`，不要另起炉灶」 |

## 两个源的字段不是一回事（这条决定了修法）

```
东财：date, main_net, small_net, mid_net, large_net, super_net      ← 四档拆分
新浪：opendate, trade, netamount, r0_net, r0_ratio, turnover, ...   ← 只有净额 + 超大单
```

新浪**没有主力/大/中/小四档拆分**。VR-GOAL-014 立过规矩：新源不提供的字段
**不返回假的 0**（当时拒绝了给 `flat` 平盘家数补 0）。这里同样适用。

## 界面上的表现

`StockData.tsx:450`：`fundFlow.length > 0 && <Metric k="近20日主力净流入" …/>`
——取不到时这个指标**直接消失**，不留任何痕迹。

## 垂直切片

- **角色**：使用看板的投研用户 / 多空辩论里的 AI
- **动作**：查个股资金流向（个股页卡片、辩论底稿、AI 工具三条路径）
- **可见结果**：主源挂了仍能拿到数据；拿到的是降级数据时会明说是哪一档降级；
  彻底取不到时说明原因，而不是静默消失

## 验收项（已确认 2026-08-03）

| # | 验收项 | 判据 | 证据形态 |
|---|---|---|---|
| 1 | 主源不可达时仍拿得到资金流 | 打桩让 `push2his` 抛异常，`/api/fund-flow` 仍返回非空且标明来源 | pytest |
| 2 | 全部源都挂时**说得出原因** | 三条源全部打桩失败 → 端点返回错误（含原因），**不是** `{"data": []}` | pytest |
| 3 | 失败不进缓存 | 首次失败后第二次请求**仍会重试上游** | pytest |
| 4 | 降级数据带标记 | 走备胎时返回体含来源/降级说明字段 | pytest |
| 5 | 不返回假的 0 | 新浪没有的四档字段**不出现**（或明确为 null），不填 0 | pytest |
| 6 | 界面上看得出来 | 个股页资金流指标在降级时有可见提示，取不到时不静默消失 | 截图 |
| 7 | 变红实验 | 撤掉降级链 → 验收项 1 变红；撤掉空守卫 → 验收项 3 变红 | 命令输出 |
| 8 | 没弄坏别的 | `./ci.ps1 -E2E` 全绿 | CI 输出 + Actions run URL |

> 判据自检：全部用打桩控制上游，**不依赖东财当下通不通**——否则这套验收明天就随风控变红变绿。
> 无时序污染、无度量污染。

> **写到这里就停，跑第一道闸。**

## 已收敛决策（拷打于 2026-08-03）

| # | 议题 | 裁决 | 理由 / 取舍 |
|---|---|---|---|
| 1 | `_cached` 缓存空结果修多宽 | **加可选 `valid` 谓词，只给资金流传** | ⚠️ **实现阶段被推翻，见验收判定①**：`astock.fund_flow` 全挂时抛异常，异常从 `fetch()` 穿出去根本走不到写缓存那行——守卫**永远不会触发**。变红实验撤掉它，测试全绿。已按 `CLAUDE.md`「从不执行的代码就是 bug 藏身处」删除，`_cached` 保持原样 |
| 2 | 降级链顺序 | `push2his` → **新浪** → `push2delay` | `push2his` 与 `push2delay` **同属东财、同一个风控面**，实测会一起挂（用户遇到的正是这个）。第二顺位若还在东财域内，在最常见的失败模式下必然也是空的——那不叫备胎 |
| 3 | 新浪字段口径 | **单独命名**（`net_amount` / `super_net`）+ 带 `source` 与说明，**不映射成 `main_net`**、缺的档位不补 0 | 新浪的 `netamount` 是净流入额，东财的 `main_net` 是「超大单+大单」，定义不同。映射过去就是**数字还在、含义变了、而且看不出来**——而「近20日主力净流入」正是个股页上唯一显示的那个数。VR-GOAL-014 已立「不返回假的 0」 |
| 4 | 界面改多宽 | **只治资金流**，其余 10 处 `.catch(() => {})` 记 backlog | 每个卡片「取不到该显示什么」是各自的产品决定，一次改 11 处 UI 而只有一条验收项盯着 = 在没有判据的情况下改十处界面 |
| 5 | 全挂时的响应 | **HTTP 502 + 各源失败原因**，不再是 200 + 空数组 | 静默的 200 正是这次的病根 |
| 6 | 降级链放哪一层 | **`astock.py` 数据层**，并删掉 `tools._fund_flow_today` | 备胎原先是 tools 私有的，所以辩论有、个股页没有——同一份数据两条路径两种结果。下沉后三条出口（页面 / 辩论 / MCP）一次都拿到。⚠️ 「删已有代码」属需显式授权动作，拷打时已声明 |
| 7 | 新浪要不要走 `em_get` | **不走** | `em_get` 是东财专用的 ≥1s 串行限流 + 代理探测，把新浪塞进那条队列只会拖慢所有东财请求。`SKILL.md` 判定新浪「风险低、不封 IP」；VR-GOAL-016 的护栏只盯 `eastmoney.com`，不冲突 |

## 不在范围内

- **`astock.py` 另外 4 处「吞异常返回空」**——其中 `eastmoney_datacenter`（:650）背后是
  6 个端点，改成抛异常会让 6 处行为一起变，各自需要验收项。记 backlog。
- **`StockData.tsx` 另外 10 处 `.catch(() => {})`**（决策 4）。
- **`_cached` 其余 9 个端点的缓存行为**（决策 1）。
- **`em_get` 的 ≥1s 串行限流本身**——backlog 第一条，要改并发，风险高。

## 合规检查

- [x] 不涉及荐股 / 预测涨跌 / 买卖时机 / 主观评分
- [x] 资金流是「用户查的那只股」的客观公开数据，不触碰打板原始池红线
- [x] 不新增落盘数据
